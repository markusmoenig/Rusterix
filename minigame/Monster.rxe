from array import array

class Monster(Entity):
    def __init__(self):
        """Initializes a Monster."""

    def event(self, event, value):
        """Event"""

        if event == "startup":
            random_walk(1.0, 1.0, 1)
